# fairino_msgs

Messages and services used by `fairino_hardware` and the FAIRINO command
clients. Build it as part of the workspace:

```bash
colcon build --symlink-install --packages-up-to fairino_hardware
```

Inspect the installed interfaces with `ros2 interface list | grep fairino`.
